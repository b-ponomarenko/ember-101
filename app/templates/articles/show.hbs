{{link-to 'Back to articles' 'articles'}}
<div class="col-8 px2 mx-auto p1 h2 center">
    <span>{{model.name}}</span>
    {{link-to "Edit info" "articles.edit" model class='btn btn-info'}}
    <a href="#" class="btn btn-danger" {{action "delete" model}}>delete</a>
</div>
